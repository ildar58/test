{
  "name": "@pam/web-session-recorder-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Web session recorder: rrweb-based, integrated via nginx proxy injection",
  "repository": {
    "type": "git",
    "url": "https://github.com/ildar58/test"
  },
  "license": "MIT",
  "author": "Ildar Dautov",
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "scripts": {
    "build": "pnpm -r build && mkdir -p replayer/vendor proxy/recorder-bundle && cp player/dist/pam-player.iife.js replayer/vendor/pam-player.iife.js && cp sdk/dist/recorder.iife.js proxy/recorder-bundle/recorder.iife.js",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "dev:ingestion": "pnpm --filter @pam/ingestion dev",
    "dev:proxy": "docker compose -f proxy/docker-compose.yml up"
  }
}
